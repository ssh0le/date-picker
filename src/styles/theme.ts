import { colors } from '@/constants/index';
const { gray, red, black } = colors;

const fontSize = {
  s: 14,
  m: 15,
};

const gap = {
  m: 8,
  l: 10,
};

const themeColors = {
  gray,
  red,
  black,
};

const padding = {
  m: 5,
  l: 10,
};

const width = {
  m: 250,
};

export const theme = {
  fontSize,
  gap,
  padding,
  width,
  colors: themeColors,
};
